<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
       body {
    font-family: 'Times New Roman', Times, serif;
    background-image: url('https://deih43ym53wif.cloudfront.net/terraced-rice-fields-sapa-vietnam-shutterstock_541224970_2b5ca32c41.jpeg');
    background-size: cover;
    background-repeat: no-repeat;
    margin: 0;
    padding: 0;
}

.container {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center; /* Đưa phần container vào giữa */
}

h1 {
    text-align: center;
    margin-bottom: 20px;
    color: white; /* Màu chữ trắng */
}

#tasks {
    background-color: rgba(255, 255, 255, 0.8);
    padding: 20px;
    border-radius: 5px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    margin-bottom: 20px;
    width: 100%; /* Đặt chiều rộng của phần tasks là 100% */
    max-width: 600px; /* Đặt chiều rộng tối đa */
}

#tasks ul {
    list-style-type: none;
    padding: 0;
}

#tasks li {
    margin-bottom: 10px;
    padding: 10px;
    background-color: #f0f0f0;
    border-radius: 5px;
}

#tasks form {
    margin-top: 20px;
    display: flex;
    justify-content: center; /* Đưa form vào giữa */
}

#tasks form input[type="text"] {
    flex: 1;
    padding: 10px;
    margin-right: 10px;
    border-radius: 5px;
    border: 1px solid #ccc;
    font-size: 16px;
}

#tasks form button {
    padding: 10px 20px;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
}

#tasks form button:hover {
    background-color: #0056b3; /* Màu nền nếu hover */
}

.sign-out {
    position: fixed;
    top: 20px;
    right: 20px;
}

.sign-out button {
    padding: 10px 20px;
    background-color: #dc3545;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
}

.sign-out button:hover {
    background-color: #c82333; /* Màu nền nếu hover */
}

</style>
</head>
<body>
    <div class="container">
        <h1>Welcome to the Dashboard</h1>
        <a href="/tasks/viewall">View My Tasks</a>
        <div id="tasks">
            <% if (tasks && tasks.length > 0) { %>
                <ul>
                    <% tasks.forEach(function(task) { %>
                        <li><%= task.description %> <button class="deleteTaskBtn" data-task-id="<%= task._id %>">Delete</button></li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No tasks found. Add a new task.</p>
            <% } %>
            <form id="taskForm">
                <input type="text" id="taskDescription" placeholder="Task Description" required>
                <button type="submit" id="addTaskBtn">Add Task</button>
            </form>
        </div>
        <div class="sign-out">
            <button onclick="location.href='/logout'">Sign Out</button>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <!-- <script>
        $(document).ready(function() {
            // Tải và cập nhật danh sách công việc khi trang web được tải
            $.ajax({
                url: '/tasks',
                method: 'GET',
                success: function(data) {
                    // Hiển thị danh sách công việc
                    data.tasks.forEach(function(task) {
                        $('#tasks ul').append('<li>' + task.description + ' <button class="deleteTaskBtn" data-task-id="' + task._id + '">Xóa</button></li>');
                    });
                },
                error: function(xhr, status, error) {
                    console.error(error);
                }
            });

            // Xử lý sự kiện gửi form
            $('#taskForm').submit(function(event) {
                event.preventDefault(); // Ngăn chặn hành vi mặc định của form

                var description = $('#taskDescription').val();
                $.ajax({
                    url: 'tasks/add',
                    method: 'POST',
                    data: { taskDescription: description },
                    success: function(data) {
                        // Cập nhật danh sách task sau khi thêm mới
                        $('#tasks ul').append('<li>' + data.task.description + ' <button class="deleteTaskBtn" data-task-id="' + data.task._id + '">Delete</button></li>');
                        $('#taskDescription').val('');
                    },
                    error: function(xhr, status, error) {
                        console.error(error);
                    }
                });
            });

            // Gửi yêu cầu xóa task khi người dùng nhấn nút "Delete"
            $(document).on('click', '.deleteTaskBtn', function() {
                var taskId = $(this).data('task-id');
                $.ajax({
                    url: '/tasks/' + taskId,
                    method: 'DELETE',
                    success: function(data) {
                        // Cập nhật danh sách task sau khi xóa
                        $('#tasks ul').empty();
                        data.tasks.forEach(function(task) {
                            $('#tasks ul').append('<li>' + task.description + ' <button class="deleteTaskBtn" data-task-id="' + task._id + '">Delete</button></li>');
                        });
                    },
                    error: function(xhr, status, error) {
                        console.error(error);
                    }
                });
            });
        });
    </script> -->
</body>
</html>